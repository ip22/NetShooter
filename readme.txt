2 НЕДЕЛЯ

Сглаживание поворота енеми.

В EnemyCharacter использовал LerpAngle в SetRotate.

Работает, как то так себе. Точнее вообще не работает) Закоментил.

Приседание.

Считываю LeftControl, если true - SitDown, если false - StandUp.
На серевер вместе с Move-датой передаю bool isSit.

Вопросы:

Когда лучше использовать подписку на изменения даты (OnChange), а когда SendMessage с оповещениями клиентов.

По гиту с ветками будет видео? А то неудобно с домашками.

3 НЕДЕЛЯ

Сделал переключение оружия.

Спавн ещё не делал. До конца недели думаю сделаю.

Не успеваю, спавн, уезжаю в отпуск на неделю) (след неделю буду на ноуте потихоньку ковырять), буду с гитом ещё разбираться чтобы к след. проекту не было такого бардака в коде, и всё чётенько было) Там в этом гите чтото с ключами какимито заморочки, это вообще нужно делать?

Стримы смотрю в записи, по времени неудобно.

По спавну была такая задумка:

Новый класс Spawner(S) на сервере, и класс SpawnPoint(SP). SP распологаются на карте и при инициализации (до появления игроков), отправляют свои коордитаты или свой индекс в S. Потом при спавне игрока, точка появления берется из списка координат (или индекса), случайно или попорядку.

Думал ещё SpawnerController ещё, со списком SP (в который они также при инициализации себя добавили), а он уже общается с S на сервере. Но подумал можно без него.
